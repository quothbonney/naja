#pragma once

namespace naja::condition {

// Runs the built-in C++ E-Flux2 conditioner (see `src/conditioning/eflux2.{h,cpp}`).
int cmd_eflux2(int argc, char** argv);

} // namespace naja::condition






