#pragma once

namespace naja::condition {

// Runs the built-in python conditioner (`src/conditioning/conditioner.py eflux ...`).
int cmd_eflux(int argc, char** argv);

} // namespace naja::condition


